import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryPurple =
      Color(0xFF820AD1); // Nubank "Ultra Violeta"
  static const Color lightBackground = Color(0xFFFFFFFF);
  static const Color darkBackground =
      Color(0xFF121212); // Um cinza escuro padrão
  static const Color darkAppBarBackground =
      Color(0xFF1F1F1F); // Um pouco mais claro que o fundo

  // Você pode adicionar mais cores aqui conforme necessário
  // static const Color accentGreen = Colors.green;
  // static const Color errorRed = Colors.red;
}
