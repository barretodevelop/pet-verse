import 'package:flutter/material.dart';

class EnterFriendCodeScreen extends StatelessWidget {
  const EnterFriendCodeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Inserir Código de Amigo')),
      body: const Center(child: Text('Tela para Inserir Código de Amigo')),
    );
  }
}
